 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>

 //  cosntantes Globais
 #define MAX_TERRITORIO 5
 #define TAM_STRING 100

// Definicao de estrutura 
struct war
{
   char territorio[TAM_STRING]
   char cor [TAM_STRING]
   int tropas;
};

// Funcao para limapr o buffer de entrada 

void limaparBufferEntrada(){
    int c;
    while ((c =getchar()) != '/n' && c != EOF)
}

// funcao principal (main)
int main(){
    struct war territorio[MAX_TERRITORIO]
    int totalterritorio = 0;
    int opcao;
}
// le a opcao do usuario.
scanf("%d", &opcao);
limaparBufferEntrada(); // Limpar o '/n' deixando pelo scanf.

